version: '3.8'

services:
  web:
    build:
      context: ./v2
      dockerfile: Dockerfile
    container_name: mind-health-services-v2
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: always
